{
    "layer": "top",
    "height": 32,
    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["clock"],
    "modules-right": ["custom/weather", "battery", "pulseaudio", "backlight", "cpu", "temperature", "memory", "network", "tray"],

    "backlight": {
      "device": "intel_backlight",
      "format": "{icon} {percent}%",
      "format-icons": ["", ""],
      "on-scroll-up": "light -A 10",
      "on-scroll-down": "light -U 10"
    },

    "battery": {
      "format": "{icon} {capacity}%",
      "format-charging": " {capacity}%",
      "format-discharging": "{icon} {capacity}% {time}",
      "format-empty": "",
      "format-full": "",
      "format-icons": ["", "", "", "", ""],
      "format-plugged": " {capacity}%",
      "format-time": "({H}:{M})",
      "states": {
        "warning": 30,
        "critical": 15
      },
    },
    },

    "clock": {
      "format-alt": "{:%A, %d %B %Y}",
      "interval": 1,
      "on-click-right": "wl-copy $(date '+%Y-%m-%d %T')",
      "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
    },

    "cpu": {
      "states": {
        "warning": 60,
        "critical": 90
      },
      "format": " {usage}%",
      "format-alt": " {avg_frequency}GHz"
    },

    "custom/weather": {
      "exec": "bash $HOME/.config/waybar/modules/weather.sh",
      "interval": 300,
      "on-click-right": "$BROWSER https://wttr.in",
      "return-type": "json"
    },

    "memory": {
      "states": {
        "warning": 60,
        "critical": 90
      },
      "format": " {}%",
    },

    "network": {
      "format-wifi": " {essid} ({signalStrength}%)",
      "format-ethernet": " {ifname}: {ipaddr}/{cidr}",
      "format-disconnected": "Disconnected",
      "on-click": "wl-copy $(ip address show up scope global | grep inet | head -n1 | cut -d/ -f 1 | tr -d [:space:] | cut -c5-)",
      "on-click-right": "wl-copy $(ip address show up scope global | grep inet6 | head -n1 | cut -d/ -f 1 | tr -d [:space:] | cut -c6-)",
      "tooltip-format": "{ifname}\n{ipaddr}/{cidr}\n",
      "tooltip-format-wifi": "{essid} {frequency}MHz\nStrength: {signaldBm}dBm ({signalStrength}%)\nIP: {ipaddr}/{cidr}\n⬆: {bandwidthUpBits} ⬇: {bandwidthDownBits}",
	  "interval": 10
    },

    "pulseaudio": {
      "format": "{icon} {volume}%",
      "format-bluetooth": "{volume}% {icon}",
      "format-muted": " MUTED",
      "format-icons": {
        "default": ["", ""]
      },
      "on-click": "pactl set-sink-mute @DEFAULT_SINK@ toggle",
      "on-click-middle": "pavucontrol-qt",
      "on-click-right": "pactl set-source-mute @DEFAULT_SOURCE@ toggle",
      "tooltip": false
    },

    "sway/mode": {
      "format": "<span style=\"italic\">{}</span>",
      "pango_markup": true
    },

	"sway/workspaces": {
      "disable-scroll": true,
      "all-outputs": true,
      "format": "{icon}",
      "format-icons": {
        "1": "",
        "2": "",
        "3": "",
        "4": "",
        "5": "",
        "6": "",
        "7": "",
        "urgent": "",
        "focused": "",
        "default": ""
      }
    },

    "temperature": {
	  "critical-threshold": 80,
      "format": "{icon} {temperatureC}°C",
      "format-icons": ["", "", "", "", ""],
      "tooltip": false
    },

    "tray": {
      "spacing": 10
    }
}