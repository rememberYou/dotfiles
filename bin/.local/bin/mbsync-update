#!/bin/bash
#
# This script automatically synchronizes the mails with mbsync.
#
# NOTE: `notify-send` needs access to the session bus and doesn't care about
# your $DISPLAY. That why, your cron won't works if `pam_systemd` doesn't do
# this yet. To be able to receive notifications from crontab, this script
# settings `XDG_RUNTIME_DIR` instead.

export XDG_RUNTIME_DIR="/run/user/$UID"
mbsync -a
